#!/bin/bash
#
# Shortcut for finding references in a php codebase
#

if [ "$#" -lt 1 ];
then
	printf "USAGE: $(basename $0) <string>\n" >&2
	exit 1
fi

find . -name '*.php' -exec grep --color "${1}" '{}' \; -print
